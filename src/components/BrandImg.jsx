import React from 'react';
import Logo from '../images/logo.png'; // Import the image correctly with the relative path

function BrandImg() {
  return (
    <img src={Logo} alt="My Image" />
  );
}

export default BrandImg;
